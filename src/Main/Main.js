function Main() {
  return (
    <>
			<p>Text example</p>
      {/* <Header data={headerData} />
      <Test />
      {goods.map(item => <Goods title={item.title} cost={item.cost} image={item.image} />)} */}
    </>
  );
}

export default Main;